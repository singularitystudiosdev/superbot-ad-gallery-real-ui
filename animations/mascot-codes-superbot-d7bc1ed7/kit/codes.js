/* codes.js: real invite codes for the flash spots. Leave empty and SB.codes() draws placeholder codes in the
   invite page's own format (16 characters over its alphabet, 0-9 plus A-Z without I, L, O, U).
   Every code shows as exactly 16 characters; dashes or spaces in a real code are dropped. Fill it with live codes before an ad that promises working ones goes out:
     window.SB_CODES = ['7K2M9QXR4TNBH3VD', ...]; */
window.SB_CODES = window.SB_CODES || [];
